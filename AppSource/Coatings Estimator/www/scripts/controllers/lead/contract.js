angular.module('estimateApp')
    .controller('ContractCtrl', [
        '$window', '$scope', '$stateParams', '$q',
        function ($window, $scope, $stateParams, $q){
            "use strict";

            function init(){}

            init();
        }]);